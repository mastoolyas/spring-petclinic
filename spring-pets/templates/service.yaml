apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-pets.fullname" . }}
  labels:
    {{- include "spring-pets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-pets.selectorLabels" . | nindent 4 }}
